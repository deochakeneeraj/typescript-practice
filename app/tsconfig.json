{
    "extends":"../tsconfig.base",
    "compilerOptions": {
        "removeComments": true
    },
    "include": ["./**/*.ts"] // this is glob to include files for compilation {uses pattern matching technique}

}